# React landing page sample

Sample landing page built with TailwindCSS for fully responsiveness and React Scroll for smooth scrolling between sections.

## Run

```sh
npm install

# start the app on a new terminal window
npm start
```
